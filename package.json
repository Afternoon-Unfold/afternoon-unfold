{
  "name": "afternoon-unfold",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').cpSync('public', 'dist', {recursive: true})\""
  },
  "devDependencies": {
    "wrangler": "^4.90.0"
  }
}
